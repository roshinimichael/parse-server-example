// Generated by Skyramp v1.3.13 on 2026-03-18 04:14:27.51020532 +0000 UTC m=+80.211988212
// Command: skyramp generate contract rest http://localhost:1337/parse/functions/health \
// 		--auth-header X-Parse-Application-Id \
// 		--framework playwright \
// 		--language typescript \
// 		--method POST \
// 		--output health_contract.spec.ts \
// 		--output-dir /home/runner/work/parse-server-example/parse-server-example/spec \
// 		--overwrite \
// 		--provider \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, checkStatusCode, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
let URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:1337");

// contract test for /parse/functions/health POST
test('testHealthPost', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["X-Parse-Application-Id"] = process.env.SKYRAMP_TEST_TOKEN;
    }

    // Request Body
    const healthPostRequestBody: string = `{}`;

    // Execute Request
    const healthPostResponse = await client.sendRequest({
        url: URL,
        path: "/parse/functions/health",
        method: "POST",
        body: healthPostRequestBody,
        headers: headers
    });

    // Generated Assertions
    expect(checkStatusCode(healthPostResponse, "20x"), 'status code').toBeTruthy();
});
