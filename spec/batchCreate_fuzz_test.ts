// Generated by Skyramp v1.3.13 on 2026-03-18 01:42:51.094499216 +0000 UTC m=+143.392996040
// Command: skyramp generate fuzz rest http://localhost:1337/parse/functions/batchCreate \
// 		--auth-header X-Parse-Session-Token \
// 		--framework playwright \
// 		--language typescript \
// 		--method POST \
// 		--output batchCreate_fuzz_test.ts \
// 		--output-dir /home/runner/work/parse-server-example/parse-server-example/spec \
// 		--overwrite \
// 		--provider \
// 		--request-data {"className":"GameScore","objects":[{"score":42,"playerName":"Alice"}]} \
// 		--response-data {"result":{"created":1,"ids":["abc123"]}} \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, getBaseUrl, getValue, iterate } from '@skyramp/skyramp';

// URL for test requests
let URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:1337");

// fuzz test for /parse/functions/batchCreate POST
test('testBatchCreatePost', async () => {
    // Invocation of Skyramp Client
    let client =  new SkyrampClient();

    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["X-Parse-Session-Token"] = process.env.SKYRAMP_TEST_TOKEN;
    }

    // Request Body
    const batchCreatePostRequestBody = `{
            "className": "GameScore",
            "objects": [
                {
                    "score": 42,
                    "playerName": "Alice"
                }
            ]
        }`
    
    // Fuzz strategies
    const batchCreatePostFuzzedBody = {
        "className": "0123456789",
        "objects": [{
            "playerName": "0123456789",
            "score": -10
        }]
    };
    // Fuzz status codes
    const expectedBatchCreatePostStatusCode: Record<string, string> = {
        "className": "40x",
        "objects": [{
            "playerName": "40x",
            "score": "40x"
        }]
    };
    // Fuzz status codes for Null values
    const expectedBatchCreatePostNullStatusCode: Record<string, string> = {
        "className": "40x",
        "objects": [{
            "playerName": "40x",
            "score": "40x"
        }]
    };

    // Execute Request
    let batchCreatePostResponse = await client.sendRequest({
        url:URL,
        path:"/parse/functions/batchCreate",
        method:"POST",
        body:batchCreatePostRequestBody,
        headers:headers,
        expectedCode:"20x"
    });


    for (const key of iterate(batchCreatePostFuzzedBody)) {
        // Execute Request
        batchCreatePostResponse = await client.sendRequest({
            url:URL,
            path:"/parse/functions/batchCreate",
            method:"POST",
            body:batchCreatePostRequestBody,
            headers:headers,
            dataOverride:{[key]: getValue(batchCreatePostFuzzedBody, key)},
            expectedCode:getValue(expectedBatchCreatePostStatusCode, key),
            description:`Fuzzing request body ${key} to ${getValue(batchCreatePostFuzzedBody, key)}`
        });


        // Execute Request
        batchCreatePostResponse = await client.sendRequest({
            url:URL,
            path:"/parse/functions/batchCreate",
            method:"POST",
            body:batchCreatePostRequestBody,
            headers:headers,
            dataOverride:{[key]: null},
            expectedCode:getValue(expectedBatchCreatePostNullStatusCode, key),
            description:`Fuzzing request body ${key} to null`
        });
    }


    expect(client.isSuccess(), 'fuzz test').toBeTruthy();
});

